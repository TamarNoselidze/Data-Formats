Query 1 (query-1-1.jq):
 - for the first hierarchy (corresponding JSON file: data-1.jsonld);
 - Counts the number of athletes born after year 2002.

Query 2 (query-2-1.jq):
 - for the second hierarchy (corresponding JSON file: data-2.jsonld);
 - Lists teams with a prize over 5000 in either league or as a winner.

Query 3 (query-3-1.jq):
 - for the third hierarchy (corresponding JSON file: data-3.jsonld);
 - Lists home team labels and values for each event.

Query 4 (query-4-1.jq):
 - for the fourth hierarchy (corresponding JSON file: data-4.jsonld);
 - Lists the winners of each sports league.